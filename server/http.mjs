import {createServer} from 'node:http';
import {timingSafeEqual} from 'node:crypto';
import {ServiceError} from './ledger.mjs';
async function jsonBody(req){let text='';for await(const chunk of req){text+=chunk;if(Buffer.byteLength(text)>8192)throw new ServiceError('요청이 너무 커요.',413);}try{return JSON.parse(text||'{}');}catch{throw new ServiceError('요청 형식을 확인해 주세요.');}}
export function createApi({ledger,verify,subjectFor,origins,unlinkAuthorization}){const allowed=new Set(origins);if(!allowed.size||allowed.has('*'))throw Error('정확한 CORS 허용 Origin이 필요합니다.');const attempts=new Map();return createServer(async(req,res)=>{const send=(status,data)=>{res.writeHead(status,{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'});res.end(JSON.stringify(data));};try{const path=new URL(req.url,'http://internal').pathname,origin=req.headers.origin;if(origin){if(!allowed.has(origin))throw new ServiceError('허용되지 않은 앱 주소예요.',403);res.setHeader('Access-Control-Allow-Origin',origin);res.setHeader('Vary','Origin');}if(req.method==='OPTIONS'){res.writeHead(204,{'Access-Control-Allow-Methods':'GET,POST,OPTIONS','Access-Control-Allow-Headers':'Content-Type,Authorization'});res.end();return;}if(path==='/health'&&req.method==='GET'){send(200,{ok:true,storage:process.env.DATABASE_URL?'postgres':'sqlite'});return;}if(path==='/v1/toss/unlink'&&req.method==='POST'){const given=Buffer.from(req.headers.authorization??''),expected=Buffer.from(unlinkAuthorization??'');if(!expected.length||given.length!==expected.length||!timingSafeEqual(given,expected))throw new ServiceError('인증이 필요해요.',401);const body=await jsonBody(req);if(!Number.isSafeInteger(body.userKey)||body.userKey<=0||!['UNLINK','WITHDRAWAL_TERMS','WITHDRAWAL_TOSS'].includes(body.referrer))throw new ServiceError('요청을 확인해 주세요.');await ledger.unlink(subjectFor(body.userKey));send(200,{ok:true});return;}if(path==='/v1/session'&&req.method==='POST'){const ip=req.socket.remoteAddress??'unknown',now=Date.now(),old=attempts.get(ip);const rate=old&&old.until>now?old:{n:0,until:now+60000};rate.n++;attempts.set(ip,rate);if(attempts.size>5000)attempts.delete(attempts.keys().next().value);if(rate.n>30)throw new ServiceError('잠시 후 다시 로그인해 주세요.',429);const subject=await verify(await jsonBody(req));send(200,{...(await ledger.login(subject)),accountId:subject});return;}const bearer=/^Bearer ([A-Za-z0-9_-]+)$/.exec(req.headers.authorization??'');const subject=await ledger.authenticate(bearer?.[1]);if(path==='/v1/quota'&&req.method==='GET'){send(200,await ledger.quota(subject));return;}if(path==='/v1/packs'&&req.method==='POST'){const body=await jsonBody(req);send(200,await ledger.reserve(subject,body.requestId));return;}throw new ServiceError('요청한 기능을 찾지 못했어요.',404);}catch(e){console.error(e);send(e instanceof ServiceError?e.status:500,{error:e instanceof ServiceError?e.message:'요청을 처리하지 못했어요. 다시 시도해 주세요.'});}});}
